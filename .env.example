# Postgres connection string. In production, set this to your Vercel Postgres
# (Neon) connection string from the Vercel dashboard / `vercel env pull`.
DATABASE_URL="postgresql://user:password@localhost:5432/nfl_dfs"
